#include <stdio.h>
int main (){
const size_t size = 1000;  /* тип компілятор визначить оптимальним чином (більшість сучасних платформ визначить long unsigned)*/
double arr[size]; // масив типу double
return 0;
}
